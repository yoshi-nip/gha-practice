# name: Schedule
# on:
#   schedule:
#     - cron: "*/5 * * * *"

# jobs:
#   run:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "Hello, world!" && date
#       - uses: actions/checkout@v4
